# Merge Notes — 2026-03-13

## Open PRs

**PR #1** — [fix: sidebar badges, inbox dismissals, project improvements, and adapter config fixes](https://github.com/alpha-community-wolf/paperclip/pull/1)
`fix/paperclip-pr-backlog-bugs` → `master`

**PR #2** — [feat: three-tier skills management system with UI and CLI install](https://github.com/alpha-community-wolf/paperclip/pull/2)
`feat/skills-management` → `master`

## Before Merging

### Shared file conflicts
Both PRs touch the same files (`packages/shared/src/index.ts`, `server/src/services/index.ts`, etc.). Merge one PR first, then rebase the other against master to resolve conflicts cleanly. The backlog PR is simpler — merge it first.

**Recommended merge order:** PR #1 first, then rebase PR #2 onto master.

### Migration numbering conflict
The backlog PR adds `0028_inbox_dismissals.sql` and the skills PR adds `0028`–`0030` for the skills tables. After merging PR #1, renumber the skills migrations to `0029`–`0031` before merging PR #2.
